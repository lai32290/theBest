version: '3'
services:
  mongo:
    image: mongo
    volumes:
      - ${PWD}/../db:/var/db
    expose:
      - "27017"
    restart: always
    container_name: theBestMongo
  node:
    image: node:6.10.3
    volumes:
      - ${PWD}:/var/local
    ports:
      - 80:3000
    links:
      - mongo:database
    restart: always
    container_name: theBestNode
    command: node /var/local/server.js

